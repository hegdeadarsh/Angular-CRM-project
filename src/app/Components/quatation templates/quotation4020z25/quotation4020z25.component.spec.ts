import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Quotation4020z25Component } from './quotation4020z25.component';

describe('Quotation4020z25Component', () => {
  let component: Quotation4020z25Component;
  let fixture: ComponentFixture<Quotation4020z25Component>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Quotation4020z25Component]
    });
    fixture = TestBed.createComponent(Quotation4020z25Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
