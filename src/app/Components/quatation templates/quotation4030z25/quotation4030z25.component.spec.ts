import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Quotation4030z25Component } from './quotation4030z25.component';

describe('Quotation4030z25Component', () => {
  let component: Quotation4030z25Component;
  let fixture: ComponentFixture<Quotation4030z25Component>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Quotation4030z25Component]
    });
    fixture = TestBed.createComponent(Quotation4030z25Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
