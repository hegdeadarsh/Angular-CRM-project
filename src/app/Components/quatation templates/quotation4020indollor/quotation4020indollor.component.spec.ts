import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Quotation4020indollorComponent } from './quotation4020indollor.component';

describe('Quotation4020indollorComponent', () => {
  let component: Quotation4020indollorComponent;
  let fixture: ComponentFixture<Quotation4020indollorComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Quotation4020indollorComponent]
    });
    fixture = TestBed.createComponent(Quotation4020indollorComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
